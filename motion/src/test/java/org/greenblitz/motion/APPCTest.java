package org.greenblitz.motion;

import org.junit.jupiter.api.Test;

public class APPCTest {


    /*@Test
    void driveValuesTest1(){
        double[] values = AdaptivePositionPursuitController.arcDriveValuesTo(new Position(-1,1,Math.PI/2), new Point(1,1), 1);
        assertEquals(values[0], 1, EPSILON);
        assertEquals(values[1], 1, EPSILON);
    }

    @Test
    void driveValuesTest2(){
        double[] values = AdaptivePositionPursuitController.arcDriveValuesTo(new Position(3,-1,-Math.PI/2), new Point(1,-3), 1);
        assertEquals(values[0], 1, EPSILON);
        assertEquals(values[1], 0.6, EPSILON);
    }

    @Test
    void driveValuesTest3(){
        double[] values = AdaptivePositionPursuitController.arcDriveValuesTo(new Position(4,20,Math.PI), new Point(0.5,20-(7*Math.sqrt(3)/2)), 3);
        assertEquals(values[0], 5.5/8.5, EPSILON);
        assertEquals(values[1], 1, EPSILON);
    }
    */
}
